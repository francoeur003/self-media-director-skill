---
name: self-media-director
description: 自媒体视频的一体化导演与增长决策能力。剪辑默认采用轻剪辑：压缩气口、突出原话中的重点、上同步字幕，不自动套画幅、BGM、Hook、动效或固定模板；只有用户明确要求时才增加包装。也用于根据抖音、视频号、小红书、B站、TikTok、Instagram、YouTube 等账号历史数据预测下一条或未来若干条视频流量，以及结合历史表现、账号目标和近期趋势，决定下一条最值得录的视频并生成拍摄 brief。典型触发语包括“帮我把视频剪辑好”“预测我的视频账号流量”“告诉我下一个视频该录什么”“从剪辑到选题一条龙”。
---

# 自媒体导演

把任务收敛成三个结果：可发布成片、诚实的流量区间、唯一的下一条拍摄建议。三种模式可独立执行；不要因为预测数据缺失而阻塞剪辑。

## 先判断模式

- `edit`：用户给出原视频或明确要求剪辑。
- `forecast`：用户给出账号历史数据，要求预测下一条或未来若干条流量。
- `next`：用户问下一条录什么、拍什么或选题方向。
- `full`：用户要求从成片、预测到下一条选题的一条龙流程。按 `edit → forecast → next` 执行。

## 输入合同

按需读取 [data-contracts.md](references/data-contracts.md)。只索取当前模式的最小输入：

- 剪辑：原视频必需。用户没有补充要求时直接采用轻剪辑默认，不追问画幅、BGM、模板或包装风格。
- 预测：至少 5 条有效历史视频数据，推荐 12 条以上。核心字段是发布时间和播放量。
- 选题：历史数据加账号赛道与目标。候选题缺失时，由 Agent 先生成 5–10 个候选；涉及“现在、近期、热点、趋势”时必须检索最新公开信息并保留来源。

## 模式一：剪好视频

1. 用 `ffprobe` 检查原视频的时长、分辨率、帧率和音视频流。
2. 转录语音并对齐时间码。只识别三类编辑点：句间气口、明确口误或重录、原话中的重点句。
3. 压缩气口，不消灭气口。保留自然呼吸和句意停顿，避免前后字粘连；阈值根据底噪和说话速度调整，不套固定秒数。
4. 突出重点。优先通过保留停顿、字幕断句和重点字幕完成，不擅自改写用户观点，不默认加缩放、音效或花字。需要重点字幕时，在对应 SRT 文本前加 `【重点】`，渲染时会自动去掉标记并使用醒目样式。
5. 上同步字幕。字幕忠于原话、短句分行、不卡嘴、不遮挡主体。
6. 默认保留原画幅、原内容顺序和无 BGM，输出到新文件。绝不覆盖原视频。
7. 用 `scripts/edit_video.py` 执行气口压缩、字幕烧录和响度整理：

```bash
python3 scripts/edit_video.py INPUT.mp4 OUTPUT.mp4 \
  --remove-silence \
  --subtitle captions.srt \
  --plan-json render_plan.json \
  --qc-json qc_report.json
```

8. 只有用户明确要求时，才使用 `--aspect`、`--edl-json`、`--music`，或增加重排 Hook、B-roll、缩放、音效与动效。
9. 用 `ffprobe` 回读成片，确认可解码、时长、原画幅、音视频流和文件大小；抽取普通字幕与重点字幕画面目检。

默认剪辑标准：

- 默认只做：剪气口、突出重点、上字幕。
- 不把所有呼吸和停顿剪光，不把口播剪成机械连珠炮。
- 不自动改画幅、不加 BGM、不重写开场、不套固定模板。
- “突出重点”服务于用户原话，不能把 Agent 自己的判断伪装成用户观点。
- 字幕必须与语音同步；重点字幕少而准，不能句句都是重点。

## 模式二：预测账号流量

1. 将平台导出或用户提供的数据整理成 CSV。不要用截图中的估算值替代可获得的原始数值。
2. 运行：

```bash
python3 scripts/forecast_traffic.py history.csv \
  --future-posts 5 \
  --out-json traffic_forecast.json \
  --out-md traffic_forecast.md
```

3. 报告下一条的 P50（中位预测）、P20–P80 区间、未来若干条累计区间、样本数、趋势、波动、回测误差和置信等级。
4. 把预测当作决策基线，不承诺结果。若样本少于 12 条、账号近期换赛道、投流规则改变、出现单条异常爆款或数据口径混杂，主动下调置信等级。
5. 平台可见曝光、播放、完播口径不同；不要把不同平台数据混成同一序列。

## 模式三：决定下一条录什么

1. 明确增长目标：`growth`（涨播放/涨粉）、`conversion`（成交/获客）或 `authority`（建立专业心智）。
2. 从历史高表现主题、近期疲劳、互动质量、趋势强度、战略匹配和制作成本生成候选表。
3. 没有现成候选时，先补充 `topic/angle/hook/payoff`；有新鲜度要求时用最新公开信息验证趋势，不凭记忆编造“热点”。
4. 运行：

```bash
python3 scripts/recommend_next_video.py history.csv \
  --candidates candidates.csv \
  --forecast traffic_forecast.json \
  --goal growth \
  --out-json next_video.json \
  --out-md next_video_brief.md
```

5. 最终只给一个主推荐，并写清：
   - 为什么现在拍它；
   - 预测流量区间与置信度；
   - 0–3 秒 Hook；
   - 逐段拍摄结构；
   - 必拍证据或结果画面；
   - CTA；
   - 成功指标和发布后复盘字段。

脚本给出的拍摄 brief 是统计骨架。Agent 必须把它改成符合用户口吻、赛道和平台的可直接开拍版本。

## Dry run

- 剪辑先加 `--dry-run --plan-json render_plan.json`，检查切段、预估时长、画幅和 FFmpeg 命令；确认无误再渲染。
- 预测与选题只读取输入并写新报告，不修改账号、不发布内容。
- 任何发布、投流、付费 API 或外部写入都必须获得对应范围的明确授权。

## QA 与停止规则

- 成片未通过 `ffprobe` 回读，不得称为“剪好了”。
- 少于 5 条有效历史数据时停止数值预测，改为输出数据模板和采集清单。
- 缺少 `topic` 且没有候选表时停止自动选题评分，先由 Agent 补候选和主题标签。
- 不得保证爆款、播放量或收入；必须给区间、假设和置信等级。
- 不得把异常爆款直接当作常态基线；不得伪造平台趋势或竞品数据。
- 脚本失败时报告失败步骤、输入、错误摘要和可恢复动作，不静默换供应商。

## 触发正例

- “把这段口播剪一下：剪气口、重点突出、字幕上好，其他别乱加。”
- “用最近 30 条抖音数据预测下一条大概多少播放。”
- “别给我十个方向，直接告诉我明天最该拍哪一条。”
- “这期剪完，再预测表现，然后安排下一期。”

## 负例

- 只写文案但不涉及视频决策：使用脚本或内容策略能力。
- 只做封面、海报或批量图片：使用视觉批处理能力。
- 要求保证爆款或保证具体播放量：拒绝保证，改做概率区间和实验设计。
- 未提供视频却要求交付成片：不能伪装已剪辑，先定位原视频。
